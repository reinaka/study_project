import { ApplicationWrapper } from "@components/page-blocks/loan-page-blocks";

export const PaymentSchedulePage = () => {
  return (
    <ApplicationWrapper
      elem="PAYMENT_SCHEDULE"
      notificationTitle="Documents are formed"
      notificationText="Documents for signing will be sent to your email"
    />
  );
};
